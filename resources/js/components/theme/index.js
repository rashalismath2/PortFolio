import { createMuiTheme, colors } from '@material-ui/core';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#6d56c1"
    },
    secondary: {
      main: "#fff"
    },
    text: {
      primary: "#555555",
      secondary:"#fff"
    }
  }
});

export default theme;